import styled from 'styled-components'

export const Container = styled.div`
  max-height: 80vh;

  button {
    padding: 10px 16px;
    margin: 5px 0 20px 0;
  }

  div {
    max-width: 40vw;
    font-size: 14px;
    word-break: break-word;
  }
`
